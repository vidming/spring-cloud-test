server:
  port: 3344

spring:
  application:
    #为这个服务取名，非常重要！！！！！
    name: config-server
  cloud:
    config:
      server:
        git:
          # uri填github上仓库地址
          uri: https://github.com/vidming/spring-cloud-test.git